# CSAA Booking System 需求文档

版本：0.3
最后更新：2026-06-25

## 1. 项目目标

本系统用于管理学生课程报名、排课、订单、教室容量，以及未来的请假、补课和调课流程。

系统分为三个入口：

- 管理员 Dashboard：管理员用于创建教室、时间、term、课程、用户/学生、订单和课程表。
- 家长手机/网页端 Link：家长用于注册登录、创建孩子、购买课程、选择时间、查询课程记录，后续提交请假/调课。
- Guest 访客端：访客可以浏览课程，但报名之前必须注册或登录。

当前优先级：

- Phase 1：完成并稳定 Web 管理后台，使用测试数据验证核心逻辑。
- Phase 1.5：完成家长手机网页端基础流程。
- Phase 2：请假/调课、提醒、补课推荐和高级教室匹配规则。

## 2. 用户角色

### 管理员

管理员可以：

- 管理教室。
- 管理时间段。
- 管理课程分类。
- 管理 term。
- 管理课程。
- 管理用户和学生。
- 查看周课程表。
- 查看课程详情和已报名学生。
- 查看和更新订单。
- 查看课程 Available / Full / Unavailable 状态。
- 后续审批请假/调课申请。

### 家长

家长可以：

- 注册和登录。
- 创建一个或多个孩子。
- 浏览课程。
- 为选中的孩子购买课程。
- 查看订单和课程记录。
- 后续提交请假/调课申请。
- 后续接收提醒。

### Guest 访客

访客可以：

- 浏览课程列表和课程详情。

访客不可以：

- 报名课程。
- 创建订单。
- 提交请假/调课。
- 查看个人课程记录。

## 3. 核心业务对象：Student （New20260511）

当前系统登录账号是家长账号，但核心业务对象应该是学生。

当前模型：

```text
User(parent) -> Child
Order -> User + Child + Class + Term
```

未来产品逻辑中，应该把 `Child` 理解为 `Student`。

推荐概念模型：

```text
Parent/User
  -> Student/Child
      -> Orders
      -> Enrollments
      -> Attendance
      -> Reschedule Requests
      -> Lesson Records
```

重要规则：

- Parent 是登录账号和联系人。
- Student 才是报名、排课、查询、调课和课时统计的对象。
- 一个家长可以有多个学生。
- 一个学生可以有多个课程和 term。
- 管理端搜索和报表应该以学生为中心，而不是以家长为中心。

管理端需要改进：

- 新增 Student 页面。
- 目前可以继续使用已有 `Child` 模型。
- 显示学生姓名、年龄、家长、电话、当前课程、term、剩余课时和备注。
- 后续可以让老师登陆 添加学生的上课表现作为记录

家长端需要的行为：

- 家长登录后先选择要操作的孩子。
- 所有报名、订单、请假、调课操作都必须带 `child_id`。

## 3.1 家长手机端 Web App

家长手机端是独立入口，不替代原有网页端。

入口：

```text
/index/mobile
/mobile
```

手机端首页优先级：

1. 先显示当前选中孩子的课程状态。
2. 显示下一节马上要上的课。
3. 显示正在进行的课程，并显示 term 起始日期和结束日期。
4. 显示待处理事项，例如待付款、已付款但等待管理员排课的订单。
5. 保留选课和调课入口，但不作为已注册家长首页第一优先级。

手机端 v1 规则：

- 底部导航点击后应尽量停留在手机端体验内。
- 原有家长网页端继续保留在 `/index/portal`。
- 手机端不应在常用操作中自动跳回旧的桌面网页页面。
- 从手机端发起登录后，登录成功应返回 `/index/mobile`。

当前 v1 限制：

- 选课、trial 体验课申请、订单支付、调课提交先以手机端区块或占位流程展示。
- 完整表单暂时仍可保留在原网页端，后续再逐步拆成真正的手机端二级页面。

## 4. Room 教室模块

管理员创建教室。

教室字段：

- 教室名称
- 容量

当前规则：

- 每个教室默认 4 个座位。
- 未来不同教室可以有不同容量。

重要容量规则：

- 满员判断应该基于共享教室占用：

```text
Room + Day + Time
```

而不是只看单个课程。

例子：

- 如果 Creator 和 Scratch 同一时间都在 Room1，它们共享同一个 4 人容量。
- 如果 Room1 周二 16:00-17:00 已经有 4 个有效学生，则该时间该教室下所有兼容课程都应显示 Full。

## 5. Time 时间模块

管理员创建标准时间段。

规则：

- 周一休息。
- 周二到周五：
  - 16:00-17:00
  - 17:00-18:00
  - 18:00-19:00
  - 19:00-20:00
- 周六、周日：
  - 09:00-10:00
  - 10:00-11:00
  - 11:00-12:00
  - 12:00-13:00 午休
  - 13:00-14:00
  - 14:00-15:00
  - 15:00-16:00
  - 16:00-17:00
  - 17:00-18:00

课程表 UI：

- 使用周视图时间网格。
- 向下滚动时保留星期表头。
- 12:00-13:00 显示为紧凑的午餐分隔行。

## 6. Category 课程分类模块

课程归属于分类。

计划分类：

- Coding
- Robotics
- Trial
- StemDay
- Math
- Language
- Competition

当前系统可能仍存在旧分类名，例如 Lego。后续需要统一命名。

## 7. Term 模块

Term 表示一个课程周期。

Term 字段：

- Term 名称
- 开始日期
- 结束日期
- 价格，可选
- 总周数/总课时数，未来字段
- 假期排除，未来字段

规则：

- Term 用于计算总课时和剩余课时。
- Term 周数应根据日期自动计算。
- 管理员必须可以手动调整周数，因为春假和不定期假期会影响实际课次数。
- 报名时应选择 term。

未来改进：

- Class 需要明确支持 term 关系。

## 8. Class 课程模块

课程字段：

- 课程名称
- Category
- Term，未来必需字段
- Room
- Day
- Time
- Price
- Status
- Cover image
- Description

当前状态概念：

- Available：管理员开放且未满员。
- Unavailable：管理员手动关闭。
- Full：根据有效学生数量和容量动态计算。

重要未来修正：

- Full 应该基于共享教室容量：

```text
Room + Day + Time active student count >= room.seat
```

有效状态通常包括：

- Paid
- Scheduled

无效状态通常不包括：

- Canceled
- Done

Pending payment 是否占位仍需确认。

## 9. 教室和课程兼容规则

这是未来调课系统的核心规则。

业务理解：

- Robotics/搭建课程通常需要固定教室和设备。
- Coding 课程相对灵活。
- 教室更像年龄/level 分组，而不是单一课程教室。
- 一个教室同一时间可以包含搭建课程和兼容 Coding 课程。

当前兼容规则草案：

| Level | 搭建课程 | 兼容 Coding |
| --- | --- | --- |
| Junior | Creator | Scratch JR / Scratch |
| Middle | Wedo | Scratch / Roblox |
| Senior | Spike | Roblox / Python / Java |

待确认问题：

- 每个 Room 属于哪个 level？
- 每个年龄对应哪个 level？
- Coding 学生是否可以跨 level？
- Trial 是否占正式座位？
- StemDay、Math、Language、Competition 是否共享同样规则？

## 10. Order 订单模块

Order 表示家长为学生购买某个课程/term。

当前状态：

- Pending payment
- Paid
- Scheduled
- Canceled
- Done

规则：

- Paid 和 Scheduled 应计入有效报名。
- Canceled 不计入。
- Done 是历史记录，不计入当前容量。
- Pending payment 是否占位仍需确认。

Order 应保留家长和学生的历史购买记录。

### 10.1 预定课程、付款、排课流程

家长端报名流程应为：

1. Guest 或家长浏览可报名课程。
2. 家长先选择课程类型。
3. 家长再选择具体星期和时间段。
4. 如果尚未登录，家长需要登录或注册。
5. 家长选择要上课的孩子。
6. 家长选择或确认 term。
7. 系统创建订单，订单状态为 `Pending payment`。
8. 家长完成付款；本地测试阶段也可以由管理员手动标记付款完成。
9. 订单状态变为 `Paid`。
10. 管理员查看 paid 订单，并确认该学生进入课程表。
11. 订单状态变为 `Scheduled`。
12. 管理员周课程表显示该课程和学生名字。

容量规则：

- `Paid` 和 `Scheduled` 订单计入当前有效占位。
- `Pending payment` 暂时不占位，除非后续业务决定需要短暂锁座。
- `Canceled` 和 `Done` 不计入当前容量，但保留历史记录。
- 同一个 `Room + Day + Time` 的有效人数达到教室容量后，课程应显示 `Full`。

管理员测试流程：

1. 创建或选择一个家长。
2. 在该家长下创建或选择一个孩子。
3. 为该孩子创建对应课程/term 的订单。
4. 将订单标记为 `Paid`。
5. 将订单标记为 `Scheduled`。
6. 检查管理员周课程表和家长订单历史。

### 10.2 Trial 试听包流程

Trial 不应作为普通 Class 直接报名。它更像一个独立的试听包/体验产品，在家长端可见，但创建订单和排课逻辑应与常规课程分开。

Trial 试听包默认包含 3 次课，当前固定价格为 CAD $98：

1. Robotics 一次。
2. Coding 一次。
3. Math 一次。

当前业务规则：

- 家长端可以看到 Trial 试听入口。
- 家长选择 Trial 后，系统不直接进入普通 Class 注册流程。
- 家长需要选择孩子。
- 系统为该孩子推荐 3 类试听时段：
  - Robotics 可用时段。
  - Coding 可用时段。
  - Math 可用时段。
- 当前第一版家长必须选择 Robotics 和 Coding 两个具体试听时段；Math 作为未来占位，暂不强制选择。
- 家长选择试听时段时，系统必须显示具体日期、星期、时间和教室。
- Trial 只有在家长完成必要选择后才生成一个 package 订单，而不是分别生成多个普通课程订单。
- Trial package 订单总金额固定为 CAD $98，订单数量显示为 3 lessons。
- Trial package 初始订单状态为 `Pending payment`。
- 家长端保留 `Pay` 按钮；付款后订单进入管理员排课流程。
- 管理员排课时应按 Trial package 中选择的每个具体课程时段分别加入对应课程。
- 推荐时段必须有空余容量，并应落在可预约时间范围内。
- Trial 最终占位发生在被选中的具体课程时段中，而不是 Trial 本身。
- 管理员课表和课程详情中，Trial 学生应显示在 `Trial Students` 中。
- Trial 学生同样占用目标 `Room + Day + Time` 的容量。
- Math 课程目前可以先保留入口和数据结构，若尚未创建可用课程，则显示 `Coming soon` 或提示联系管理员。
- `Pending payment` 的 Trial 不应占用主课表容量；只有付款并由管理员排课后才显示在主课表和课程详情中。
- Trial 学生在主课表上应有可识别标记，并在课程详情的 `Trial Students` 选项卡中显示具体日期。

推荐规则初稿：

- Robotics 试听优先推荐 Robotics 分类下有空位的课程。
- Coding 试听优先推荐 Coding 分类下有空位的课程。
- Math 试听未来推荐 Math 分类下有空位的课程。
- 不推荐已经 Full 的时段。
- 不推荐与该学生已有 active class、已付款待排课 Trial、已排课 Trial 或已确认补课冲突的时段。
- Trial 内部两个已选时段之间也不能冲突；如果选择了冲突时段，系统应提示并允许用户重新选择另一列的时段。
- 后续可根据学生年龄/level 优化推荐。

已实现/当前实现建议：

1. 使用 Trial request 记录 package 下的具体试听选择。
2. 家长端 Trial 选择页显示 Robotics、Coding 和 Math 占位三列。
3. 每列默认展示最近的可选时段，避免页面过长。
4. 提交后生成一个 Trial package 订单。
5. 家长付款后，由管理员从订单页排课。
6. 排课后将学生加入对应课程的 `Trial Students`。
7. 主课表显示试听学生并计入容量。

## 11. Schedule 课程表模块

管理员课程表是核心运营视图。

当前功能：

- 默认周视图。
- 左侧时间轴。
- 星期列。
- sticky 星期表头。
- 紧凑课程行。
- 课程颜色区分。
- 行内显示学生摘要。
- hover 下拉显示学生名字，一行一个。
- 午餐分隔行。
- FULL 标记。
- 点击课程进入课程详情。
- 按学生姓名搜索，忽略大小写。
- 主表顶部搜索和周/月切换区域在滚动时保持 sticky。
- 星期表头在纵向滚动时保持 sticky。
- 点击课程时携带具体日期进入课程详情，用于过滤当天的普通学生、补课学生、试听学生和请假/取消记录。
- 当课程详情当天存在 Rescheduled / Trial / Absent 学生时，对应选项卡显示数量标记。
- 有新的已付款待排课订单时，左侧 Order 菜单显示红点提醒。
- hover 展开的学生名单不应阻挡课程卡片点击。

未来改进：

- 增加按教室、分类、term、学生筛选。
- 更清楚地显示 room grouping。
- 支持学生个人课表视图。
- 增加管理员手动 note 标记，并在主表卡片/详情中显示。

## 12. 查询模块

系统需要以学生为中心的查询功能。

需要查询的字段：

- 课程名称
- 上课时间
- 学生姓名
- 家长姓名
- 家长电话
- Term
- 课程开始日期
- 课程结束日期
- 总课时数
- 已使用课时数
- 剩余课时数
- 订单状态
- 请假/调课记录
- 课程状态：正在进行 / 已结束 / 已取消 / 待付款 / 已排课
- Trial / Rescheduled / Absent 标记
- 课程具体日期、星期、时间和教室

推荐后台查询结果：

| Student | Parent | Course | Day/Time | Term | Total Lessons | Used | Remaining |
| --- | --- | --- | --- | --- | --- | --- | --- |

学生模块显示规则：

- 列表页显示最核心信息：学生姓名、年龄、当前课程摘要。
- 课程摘要需要包含课程名称、日期/星期、时间和教室。
- 详细页显示学生全部课程记录，包括 active classes、finished classes、trial classes、rescheduled classes 和 canceled/absent records。
- 已结束课程必须保留为历史记录，不从学生档案中删除。
- 学生姓名搜索应忽略大小写。

## 13. 请假、补课、调课

Phase 2 功能。

规则：

- 家长必须至少提前 48 小时请假。
- 学生在课程/term 结束前有 2 次调课机会。
- 请假通过后应生成一次补课/调课资格。

调课推荐需要考虑：

- 学生年龄/level。
- 原课程类型。
- 目标教室兼容性。
- 目标时间容量。
- 搭建课程固定教室规则。
- Coding 课程灵活教室规则。
- 共享教室占用人数。

调课记录字段：

- Student
- Parent
- Original class
- Original date/time
- Original term
- Request type
- Reason
- Requested target date/time
- Assigned target class
- Status: pending / approved / rejected / completed / canceled
- Admin note
- Created time
- Updated time

课程表显示：

- 正常学生正常显示。
- 调课学生使用特殊标记，例如绿色星号。
- 请假学生和试听学生在课程详情中单独显示。

## 14. 家长手机网页端

第一版手机端应做成 mobile web app，不先做原生 App。

Phase 1.5 手机端功能：

- 登录/注册。
- 选择孩子。
- 创建孩子。
- 浏览课程。
- 查看课程详情。
- 为选中的孩子报名。
- 选择 term。
- 查看订单。
- 查看孩子当前课程。

Phase 2 手机端功能：

- 请假申请。
- 调课申请。
- 选择补课选项。
- 接收提醒。

## 15. 提醒功能

高级功能。

提醒类型：

- 上课提醒。
- Term 即将结束提醒。
- 剩余课时提醒。
- 补课提醒。
- 调课处理结果提醒。
- 推荐课程提醒。

可能渠道：

- 站内消息。
- Email。
- SMS / WhatsApp / WeChat，后续决定。

## 16. 课程推荐

高级功能。

推荐依据：

- 学生年龄。
- 当前 level。
- 已完成课程。
- 可上课时间。
- 教室容量。
- 剩余课时。
- 家长购买历史。

## 16.5 调课、取消课程、补课工作流（Phase 2 重点）

本模块先在 Web 家长端完成业务闭环，确认逻辑稳定后再迁移到手机端/mobile web。第一阶段不追求一次性完成全部功能，应先完成可测试的最小闭环。

### 16.5.1 核心规则

- 家长发起取消/调课申请必须至少提前 48 小时。
- 如果距离上课不足 48 小时，系统应提示家长通过电话或邮件直接联系管理员处理特殊情况。
- 每个学生在一个 term 内可调课次数需要被记录；当前初步规则为 term 结束前有 2 次调课机会。
- 已批准的取消课程会生成一次可补课机会。
- 补课必须安排在原订单/term 的日期范围内，除非管理员手动特批。
- 补课学生会占用目标教室人数容量。
- 管理员确认排课后，补课学生显示在目标课程的 Rescheduled Students 中，并在主课表中使用特殊标记。
- 原课程应保留历史记录，不能直接删除购买和排课历史。

### 16.5.2 家长端流程

家长端应从“已购买且正在使用的课程”进入调课流程。

推荐入口：

- My Orders / My Courses 中显示学生当前 active classes。
- 家长选择某个孩子。
- 家长进入某个已购买、已付款、正在使用的课程。
- 页面显示课程日历，能看到该学生在 term 内的上课日期。

家长端取消课程流程：

1. 家长选择孩子。
2. 家长进入该孩子正在上的课程。
3. 家长在课程日历中选择要取消的某一次课。
4. 系统检查是否提前 48 小时。
5. 如果不足 48 小时，提示家长电话/邮件联系管理员，不允许直接提交普通申请。
6. 如果满足 48 小时，家长点击 Cancel Class。
7. 系统弹出确认窗口，说明取消后需要管理员审批。
8. 家长确认后，系统创建取消课程申请。
9. 系统发送取消课程申请邮件给管理员。
10. 申请状态为 Pending admin review。

管理员批准取消后：

1. 原课程该学生本次课标记为 absent/canceled by request。
2. 系统生成一次 makeup eligibility。
3. 家长端显示可选择补课。
4. 系统默认推荐 2 个可选补课时段。

家长端补课申请流程：

1. 家长看到系统推荐的 2 个可选时段。
2. 推荐时段必须在 term 范围内。
3. 推荐时段必须满足教室容量。
4. 推荐时段应尽量匹配课程类型、年龄/level 和教室兼容规则。
5. 家长选择一个补课时段。
6. 家长确认提交补课申请。
7. 系统发送课程调整申请给管理员。
8. 申请状态为 Pending admin review。

### 16.5.3 管理员端流程

管理员需要新增课程调整管理模块，建议命名为 Reschedule Requests 或 Course Adjustments。

管理员模块需要显示：

- Request ID
- Student
- Parent
- Parent phone/email
- Original class
- Original class date/time
- Original term
- Request type: cancel class / makeup class / admin manual reschedule
- Request reason
- Request status
- Recommended makeup options
- Selected makeup option
- Admin decision
- Admin note
- Created time
- Updated time

管理员处理取消课程申请：

1. 查看家长提交的取消申请。
2. 检查是否满足 48 小时规则。
3. 可批准或拒绝。
4. 批准后，系统记录原课程取消/请假状态。
5. 系统生成补课资格。
6. 系统给家长端开放补课选择。
7. 系统可发送审批结果通知给家长。

管理员处理补课申请：

1. 查看家长选择的补课时段。
2. 检查目标课程是否在 term 范围内。
3. 检查目标教室容量。
4. 检查课程类型、年龄/level、教室兼容规则。
5. 管理员批准后，系统将学生加入目标课程 Rescheduled Students。
6. 目标课程人数容量增加。
7. 主课表显示该学生为调课/补课学生。
8. 系统通知家长申请已确认。

### 16.5.4 系统推荐补课时段规则

系统默认推荐 2 个可选补课时段。推荐逻辑初稿：

- 必须在原订单 term 的 start/end 日期范围内。
- 必须是当前日期之后的课程。
- 必须有可用容量。
- 必须不与学生已有课程冲突。
- 优先推荐同课程名称、同课程类型的时段。
- 如果同课程无可用时段，再考虑兼容课程/教室。
- Robotics/building 课程优先固定教室和设备。
- Coding 课程可根据年龄/level 和容量更灵活推荐。
- 推荐结果应显示课程名、日期、星期、时间、教室、剩余座位。

已确认规则：

- 不允许补到不同课程名但同 level 的课程；补课应尽量保持同课程名。
- 默认不允许跨 term 补课。
- 如果学生已经报名了下一个 term，管理员可以允许补课延期到下一个 term。
- 如果家长不接受系统推荐的 2 个时段，家长可以在申请留言中说明偏好，或直接电话联系管理员。
- 家长留言/电话沟通后的特殊安排由管理员手动标注和处理。
- 管理员需要可以手动添加第三个推荐补课选项。
- 管理员添加的第三个选项应同样检查容量、term 范围和课程冲突；如需强制安排，必须填写 admin note。

### 16.5.5 管理员手动调课

管理员需要有手动调课能力，用于电话/邮件特殊情况或不足 48 小时的请求。

管理员手动调课初步流程：

1. 管理员搜索学生。
2. 管理员查看该学生当前 active classes 和课程日历。
3. 管理员选择原课程日期。
4. 管理员选择目标补课日期/课程。
5. 系统提示容量、term 范围、课程兼容性和冲突检查结果。
6. 管理员可以强制确认，但需要填写 admin note。
7. 系统生成 admin manual reschedule 记录。
8. 学生进入目标课程 Rescheduled Students。
9. 目标课程占用容量。
10. 家长端能看到调整后的补课记录。

### 16.5.6 管理员 Note 和课表标记

管理员需要可以在课程调整过程中添加 note，用于记录电话沟通、特殊原因、手动安排和例外审批。

Note 显示需求：

- Course Adjustments 列表显示 admin note 摘要。
- 调课详情页显示完整 admin note 和 parent note。
- 主课表中被管理员特殊标记的课程/学生应显示 note 标识。
- hover 或点击课程详情时可以查看 note 内容。
- note 不应替代正式状态；状态仍然使用 pending / approved / rejected / completed / canceled。

### 16.5.7 数据模型草案

后续可能需要新增 RescheduleRequest / CourseAdjustment 模型。

建议字段：

- id
- student/child
- parent/user
- original_order
- original_class
- original_lesson_date
- original_day
- original_time
- original_term
- request_type
- request_reason
- request_source: parent / admin
- status: pending / approved / rejected / completed / canceled
- recommended_options
- admin_extra_recommendation
- selected_target_class
- selected_target_date
- selected_target_day
- selected_target_time
- selected_target_room
- admin_note
- parent_note
- approved_by
- approved_time
- created_time
- updated_time

### 16.5.8 分阶段实现建议

第一步：需求和数据模型确认。

第二步：家长端在已购买课程中显示课程日历和 Cancel Class 按钮。

第三步：创建取消课程申请，不直接改排课。

第四步：管理员 Course Adjustments 列表可审批取消申请。

第五步：批准后生成补课资格，并给家长推荐 2 个补课时段。

第六步：家长选择补课时段并提交申请。

第七步：管理员审批补课申请并排课。

第八步：主课表和课程详情显示 Rescheduled Students，并计入容量。

## 17. 当前已完成内容

已完成或部分完成：

- 本地前后端运行。
- GitHub 仓库。
- Admin login。
- 基础 Room / Time / Category / Term / Class / User / Order 页面。
- 周课程表。
- 课程详情学生列表。
- Paid / Scheduled / Done 状态区分。
- Schedule FULL 标记。
- Class 列表动态状态。
- Student 管理页面，暂时沿用现有 Child 模型。
- Time 排序。
- 本地测试数据创建。
- 本地图片文件恢复。
- 家长端课程选择页按课程名称聚合，同名课程使用统一图片。
- 家长端 Trial package 选择页。
- Trial package 订单、付款和管理员排课流程。
- 主课表 Trial 学生显示和 Trial Students 选项卡。
- 主课表按学生姓名搜索。
- 主课表和星期表头 sticky。
- 课程详情页 Rescheduled / Trial / Absent 选项卡数量标记。
- 左侧 Order 红点提醒 paid but unscheduled 订单。
- 调课申请基础闭环：家长取消申请、管理员审批、生成补课资格、推荐补课时段、管理员确认补课排课。
- 补课推荐避免推荐学生原本已有课程的时段。

## 18. 已知风险

- Full 逻辑已开始使用容量判断，但仍需要在更多真实测试数据下验证共享 Room + Day + Time 的边界情况。
- Class 需要更清楚的 term 关系。
- 测试数据仍可能存在历史遗留状态，需要持续清理冲突订单、重复试听和不合理排课。
- 上传图片是本地文件，不会自动进入 GitHub。
- 家长手机端尚未正式开始，目前先在 Web 端验证业务流程。
- Trial package 目前 Math 仍为占位，后续创建 Math 课程后需要补齐选择和排课逻辑。
- AWS / 公网 demo 部署仍处于早期验证，生产化需要持久化进程、静态资源、数据库备份和权限控制。
- 永久换课目前已具备基础闭环，但候选课程列表和回退入口仍偏 demo/运营验证，需要继续做成完整记录列表和更清楚的校验提示。

## 19. 建议下一步

Web 管理后台：

1. 继续验证 Full、Trial、Rescheduled、Absent 四类学生是否都正确计入容量和详情页。
2. 完善 Student detail，显示完整 active/finished/trial/rescheduled/canceled 课程历史。
3. 明确 Class-Term 关系，并减少历史数据中的临时 term。
4. 增加管理员 note 标记，支持特殊调课、电话沟通和人工例外处理。
5. 按真实教室兼容规则继续清理和补充测试课程数据。
6. 持续更新本文档和测试用例文档。

手机端：

1. 家长登录/注册。
2. 孩子选择和孩子管理。
3. 课程列表和课程详情。
4. 报名流程。
5. 订单/课程记录页面。

Phase 2：

1. 请假申请。
2. 调课申请。
3. 补课推荐。
4. 提醒系统。
5. 课程推荐。

## 20. 近期确认需求记录（2026-05-14）

本节记录最近一轮已经确认但仍可能继续细化的需求，便于后续验收和同步。

### 20.1 主课表提醒和标记

- 主课表需要支持按学生姓名搜索，搜索忽略大小写。
- 主课表滚动时，顶部工具栏和星期表头都需要保持可见。
- 课程卡片上显示学生摘要；hover 展开时只显示学生名字，一行一个。
- 如果当天该时段有补课学生，课程详情的 `Rescheduled Students` 选项卡显示数量标记。
- 如果当天该时段有 Trial 学生，课程详情的 `Trial Students` 选项卡显示数量标记。
- 如果当天该时段有取消/请假记录，课程详情的 `Absent Students` 选项卡显示数量标记。
- 左侧 Order 菜单在存在 `Paid` 但未 `Scheduled` 的订单时显示红点。

### 20.2 订单和排课边界

- `Pending payment` 订单不进入主课表，不占用容量。
- `Paid` 订单代表家长已付款，但仍需要管理员排课。
- 只有管理员确认排课后，学生才出现在主课表和课程详情中。
- Trial package 虽然包含多个具体课程时段，但在家长订单中只显示为一个 package 订单。
- 管理员排课时，需要按 package 中的每个选择分别安排到对应课程。

### 20.3 学生档案

- 学生是系统后续查询和运营的中心对象。
- Student 列表只显示基础摘要，避免页面过宽。
- Student detail 需要显示：
  - 家长和联系方式。
  - 当前正在进行的课程。
  - 已结束课程。
  - Trial 记录。
  - Rescheduled 记录。
  - Absent / canceled 记录。
  - 每条课程记录的 term、日期、星期、时间、教室和状态。

### 20.4 Trial package

- Trial 目前按 package 售卖，固定 CAD $98 / 3 lessons。
- 第一版实际需要家长选择 Robotics 和 Coding；Math 暂时显示 Coming soon。
- 每个可选时段必须显示具体日期，不只显示星期。
- Trial 内部选择不能出现时间冲突。
- Trial 学生在管理员排课后进入主表，并显示在 `Trial Students` 选项卡。

## 21. 近期确认需求记录（2026-06-25）

本节记录今日围绕新版 Schedule、学生档案、临时/永久调课新增并已部分实现的设计规则。后续单独编写使用手册时，应把本节拆成管理员操作步骤和家长端说明。

### 21.1 Daily Schedule 新版运营视图

Schedule 页面从传统周网格改为以“某一天”为核心的运营视图。

当前规则：

- 默认查看某一天的所有课程安排。
- 左侧日期栏显示当前周可查看日期，便于快速切换日期。
- 右上角保留 Week 入口，方便快速定位某一周。
- 周一无课，不在常规课程表中显示。
- 周二到周五显示 16:00-20:00 课程时间。
- 周六、周日显示 09:00-18:00 课程时间。
- 12:00-13:00 为午餐/休息时间，不安排课程。
- 如果某天没有课程，可以在日期栏显示日期，但下方应明确标注无课。
- 每一列代表一个教室。
- 每一行代表一个上课时间段。
- 每个单元格显示该日期、该教室、该时间段内的课程、老师和学生信息。
- 没有课程的时段可以不显示，减少页面高度。

显示规则：

- 顶部 Announcement 用一行展示，所有老师都可以看到。
- Room header 一行展示教室、老师和座位信息，例如 `Room1 · TeacherA · 4 seats`。
- 一个教室使用一种稳定颜色，便于老师快速识别自己的教室。
- 课程标题和老师名称尽量压缩在一行，例如 `Creator · TeacherA`。
- 学生姓名一行一个，避免被截断或难以扫描。
- 学生可带标记，例如 Trial、Rescheduled、Absent、Note，后续可继续细化颜色和图标。
- 鼠标 hover 学生姓名时应显示 term 信息和管理员额外 note。
- 课程已满不再只显示 `Full`，应改为更适合运营场景的容量状态，例如 `At capacity` 或显示 `4/4 seats`。

性能要求：

- 首次加载不能因为学生详情、历史订单或评论记录过多而明显变慢。
- Schedule 页面只加载当天运营需要的数据。
- 学生完整档案、内部 comment、历史课程应在点击学生后按需加载。

### 21.2 老师和教室归属

当前先使用预设老师数据满足 demo 和运营沟通需求。

规则：

- 老师归属到具体日期、教室和时间段。
- 同一个老师名称未来可能会更改，因此不要把老师写死到学生或订单历史中。
- 老师信息先服务于 Schedule 显示和当天运营安排。
- 后续如果引入老师登录，则老师只能看到或优先看到自己负责的教室/时间段。

待确认：

- 老师是否需要独立账号。
- 老师是否可以直接写学生课堂表现。
- 老师是否可以看到家长联系方式。

### 21.3 学生 note、comment 和课堂表现

当前区分三类记录：

- Schedule note：只服务于当天课程表显示，例如临时备注、电话沟通、特殊提醒。
- Student internal comment：管理员在学生档案中记录内部评论，长期保留。
- Performance record：未来用于记录每次课的课堂表现，应挂在 Student 和具体 lesson date 上。

已确认规则：

- note 只显示在 Dashboard/Schedule 上，不等同于正式课堂表现。
- comment 放在 Student 信息中，作为内部长期记录。
- performance 将来放在 Student 信息中，按学生和具体上课日期查询。
- 点击 Schedule 中的学生姓名，应进入该学生详情，而不是只显示当天信息。
- 关闭学生详情后，应回到原 Schedule 日期和视图状态。
- Student 页面搜索应支持学生真实显示名，例如 `Ivy Demo`，避免因为内部用户名或大小写导致找不到。

性能要求：

- Student 列表只加载摘要信息。
- 点击学生详情时再加载课程历史、请假/补课记录和内部 comment。
- 学生表现记录未来可能增长很快，必须支持按学生、课程、term、日期分页查询。

### 21.4 手动调整课程：单日调整

管理员可在 Schedule 页面进入 Manual Adjustment 模式，对某一天做临时调整。

规则：

- 单日调整只影响选中的具体日期。
- 单日调整不修改原始订单、不改变学生长期课程信息。
- 移动学生后必须点击保存才正式生效。
- 保存前应支持回退/撤销。
- 保存后系统应记录调整来源、原课程、目标课程、日期和管理员备注。
- 如果是临时病假/请假，可单独标记为 sick leave 或 absent。
- 病假是否扣课时由管理员选择；如果扣课时，需要更新学生剩余课时。
- 取消/请假学生不应同时出现在 Normal Students 和 Absent Students。
- 非当天学生不应出现在当天课程详情中。

容量和冲突规则：

- 目标教室/时间段必须检查容量。
- 调整后的学生不能和自己当天已有课程时间冲突。
- Trial、Rescheduled、Absent 等特殊状态应继续被课程详情正确分类。
- 单日调整后的 Schedule 和课程详情必须保持一致。

### 21.5 手动调整课程：永久换课

当学生在 term 中途需要长期更换上课时间时，管理员使用 `All future classes` 进行永久换课。

业务规则：

- 永久换课从某个 effective date 开始生效。
- 永久换课只影响 effective date 及之后的课程。
- effective date 之前的上课历史保留在原订单和原课程中。
- 永久换课不直接覆盖原订单，而是拆分为旧订单和新订单两段历史。
- 原订单的结束日期被缩短到 effective date 前一天。
- 新订单继承同一个学生、家长、term、付款信息和剩余课时。
- 新订单进入目标课程，并作为该学生后续长期课程。
- 系统需要记录 Permanent Course Change，便于查询和回退。
- 管理员应能回退最近一次永久换课；回退后恢复原订单和原课程关系，目标订单取消。

校验规则：

- 目标课程不能和原课程相同。
- 目标课程必须在 effective date 之后仍有可上课日期。
- 目标课程必须有容量。
- 目标时间不能和该学生已有 active class 冲突。
- 如果学生没有新的 term，默认只能在本 term 范围内换课。
- 如果跨 term 需要特殊处理，必须由管理员手动确认并留下 admin note。

当前实现限制：

- 目标课程候选列表可能先展示可选课程，最终保存时再做严格容量和冲突校验。
- 回退入口目前偏 demo/运营验证，后续应做成完整的永久换课记录列表。

### 21.6 课程详情和学生分类一致性

课程详情必须以具体日期为过滤条件，不能只按课程长期名单显示。

规则：

- Normal Students 只显示该日期真正应该正常上课的学生。
- Rescheduled Students 只显示该日期补课/调课进来的学生。
- Trial Students 只显示该日期试听学生。
- Absent Students 只显示该日期取消、请假或病假的学生。
- 同一个学生在同一天同一课程中只能属于一个主要分类。
- 上一级 Schedule 显示的取消/请假状态，进入课程详情后必须一致。
- 课程详情 tab 的数量标记应和列表内容一致。

### 21.7 家长端同步要求

家长端需要逐步同步学生课程和调课信息。

规则：

- 家长端订单卡片必须显示孩子信息，尤其一个家长有多个孩子时。
- 家长端应有单独的 Choose Classes / 选课入口。
- 已购买课程中应显示孩子、当前课程、最近一次课程、是否存在调课或请假信息。
- 调课、请假、补课信息应同步到家长端，让家长能看到申请结果和后续安排。
- 移动端首页不应显示过多无关课程；点击底部选课入口后再进入课程选择。
- 多子女家庭需要可以切换孩子，所有订单、课程、调课记录都按选中孩子过滤。

### 21.8 Demo 数据和测试要求

为便于汇报和测试，需要保留较完整的 demo 数据。

规则：

- Demo 数据应包含多名家长、多名学生、多子女家庭、不同课程、不同教室、不同 term。
- 至少包含正常学生、试听学生、请假/取消学生、补课学生、已满教室和空教室。
- Schedule 当前日期应有足够学生，用于测试 room color、容量、note、学生详情、单日调整和永久换课。
- Demo 登录信息需要稳定记录，避免汇报前无法登录。
- 测试数据不能破坏核心唯一关系：学生 + 课程 + 时间 + term 应能形成可追踪记录。

### 21.9 后续使用手册计划

后续需要单独编写一份使用手册，不建议继续放在本设计文档内。

建议手册结构：

1. 管理员登录和基础设置。
2. 创建 Room、Time、Term、Class。
3. 家长/学生和订单管理。
4. Schedule 日常查看。
5. 单日手动调整课程。
6. 永久换课。
7. 请假、取消和补课处理。
8. 学生详情、comment 和未来 performance 记录。
9. 家长端常见操作。
10. Demo 测试账号和汇报检查清单。
